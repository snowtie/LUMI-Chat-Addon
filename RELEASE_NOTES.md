# LUMI Chat Addon v1.1.2

## 수정 사항

- GPT-SoVITS를 선택해도 Fish Audio의 `no-tts-key` 오류가 나던 문제 수정. 원본 TTS 함수에 메모리상 분기를 적용하여 JAR 로딩 순서에 의존하지 않도록 변경했습니다. 원본 JAR 파일은 수정하지 않습니다.
- LUMI Chat 1.3.0의 설정 저장 함수 변경에 대응. 저장 성공 때만 애드온 설정을 반영하며 기존 반환값과 검증 실패 처리를 보존합니다.
- 사용자 지정 TTS와 GPT-SoVITS 전환 시 다른 프로바이더의 설정 카드가 섞이지 않도록 수정했습니다.
- Claude 상태 확인·대화·로그아웃 중 명령창이 깜빡이는 문제 수정. 사용자가 직접 진행하는 로그인·설치 창은 계속 표시합니다.
- Claude 모델 전달, 인증 실패, 사용량 제한 및 응답 오류 처리를 개선했습니다. 로그인 완료가 확인되기 전에는 기존 두뇌 선택을 바꾸지 않습니다.
- ChatGPT와 Claude의 사용자 지정 모델을 재실행 시 보존하고, 설정 파일 읽기 실패 시 기존 파일을 빈 설정으로 덮어쓰지 않도록 개선했습니다.
- 다운로드 실패 시 기존 파일 보존, GPT-SoVITS 동시 요청 직렬화, 빈 음성 데이터 오류 처리를 추가했습니다.
- 앱 종료 시 도우미와 관리 중인 GPT-SoVITS가 함께 종료되도록 개선했습니다. 집중 모드 무음 및 음성 절전 정책은 유지합니다.

## 업데이트 안내

창작마당은 별도로 갱신됩니다. GitHub 패키지를 직접 사용하는 경우 `LUMI-Chat-Addon-v1.1.2-workshop.zip`의 내용을 내 모드 폴더에 적용하세요. Little LUMI를 다시 시작한 뒤 모드 탭에서 코드 실행 승인이 나오면 확인해 주세요. 같은 애드온의 내 모드와 구독본을 동시에 활성화하지 마세요.

계정 도우미는 플러그인이 이 릴리스에서 내려받아 SHA-256을 검증합니다. 음성 가중치는 기존 v1.1.0 자산을 계속 사용합니다. v1.0.9 이하 설치형 앱을 정리하는 기존 제거기도 함께 제공합니다.

## 검증

- Rust 단위 테스트 30개
- JAR 로딩 순서, 최초 로딩 및 재변환에 따른 TTS 경로 회귀 테스트
- 구형·신형 설정 저장 반환값, TTS 화면 전환, 다운로드 실패 시 기존 파일 보존 테스트
- Claude 명령창 숨김·모델 전달·인증 오류·시간 제한 모의 실행 테스트
- 도우미 종료 및 배포 패키지·체크섬 검사
- 릴리스 준비 전 실제 Little LUMI + LUMI Chat 1.3.0에서 3.02초 WAV 생성 확인. Claude 실제 계정 대화는 검증하지 않았습니다.
